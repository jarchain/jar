/-!
# Services — §9, §12, §14

- Service account model (eq 9.3)
- Minimum balance computation (eq 9.8)
- Privileged services: manager, assigner, designator, … (eq 9.9)
- Work-package structure `P` (eq 14.2)
- Work-item structure `W` (eq 14.3)
- In-core computation pipeline (§14)
- Work-report computation `Ξ(p, c)` (eq 14.12)
- Auditing protocol (§17)
-/
