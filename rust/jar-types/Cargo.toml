[package]
name = "jar-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared types for the JAR minimum kernel."

[dependencies]
thiserror = { workspace = true }
